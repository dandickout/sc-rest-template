{
  "name": "sc-get-list_methods",
  "version": "1.0.0",
  "dependencies": {
    "mongodb": "^4.3.1"
  },
  "description": "gets the list of method names and ids",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
